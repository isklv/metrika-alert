# metrika-alert

Мониторинг Яндекс.Метрики: каждые 10 минут сравнивает визиты и достижения целей
с тем, что для этого времени нормально, и шлёт алерты в **Telegram**,
**ВК Тимс** и на **webhook**.

Один процесс, SQLite, только исходящие соединения — работает за NAT.

> **Как считается «нормально».** Измеряется скользящее часовое окно, которое
> сдвигается каждые 10 минут. Вторник 14:20–15:20 сравнивается с тем же
> промежутком прошлых вторников — по умолчанию за четыре недели, по медиане.
>
> Окно часовое, а шаг десятиминутный не случайно: у трафика сильный суточный и
> недельный ритм, плоский порог его не различает, а голая десятиминутка держит
> шестую часть трафика и скачет слишком сильно, чтобы порог в процентах что-то
> значил. Час даёт устойчивую цифру, шаг — быструю реакцию.

## Быстрый старт

```bash
cp config.example.yaml ~/.metrika-alert/config.yaml
# вписать bot_token и admin_ids
go build -mod=vendor -o metrika-alert ./cmd/server
./metrika-alert
```

Конфиг ищется в таком порядке: путь из аргумента, `~/.metrika-alert/config.yaml`,
`./config.yaml`.

Docker:

```bash
docker compose up -d --build
```

`--build` обязателен и при обновлении: без него compose переиспользует старый
образ. Какая сборка запущена, показывают первая строка лога и команда
`/version`.

**Полная инструкция по развёртыванию — [DEPLOY.md](DEPLOY.md)** (Docker Compose,
systemd, настройка ботов, резервное копирование, диагностика).

## Каналы доставки

| Канал | Настройка | Адрес destination |
|---|---|---|
| Telegram | `telegram.bot_token` от [@BotFather](https://t.me/BotFather) | числовой `chat_id` |
| ВК Тимс | `vkteams.bot_token` от `@Metabot` | строковый chatId, например `user@corp.example` |
| Webhook | не требует бота | URL для `POST` с JSON |

Каналы независимы: пустой токен просто отключает канал. ВК Тимс работает и с
облаком, и с on-premise — эндпоинт задаётся в `vkteams.base_url`.

Пока ни один destination не создан, алерты уходят администраторам из `admin_ids`.
Как только появился первый destination, рассылка админам прекращается.

## Команды бота

Одинаковы в Telegram и в ВК Тимс.

```
/counters            список счётчиков
/addcounter          добавить счётчик Метрики
/goals <id>          цели счётчика и их ID
/triggers <id>       правила алертов
/addtrigger <id>     создать правило
/actions             куда идут алерты
/addaction           добавить destination
/alerts [id]         история алертов
/reports <id>        какие отчёты настроены
/addreport <id>      отчёт по страницам и целям
/deletereport <id>   удалить отчёт
/report [id]         запустить отчёты сейчас
/schedule [когда]    когда присылать отчёты
/poll                опросить все счётчики один раз
/whoami              показать свой ID
/version             версия запущенной сборки
/help                меню
```

Команды принимаются только от пользователей из `admin_ids`. **Пустой список
запрещает всё**: в счётчиках хранятся OAuth-токены Метрики, поэтому токен бота
сам по себе не считается достаточной авторизацией. Бот в ответ на отказ покажет
ваш ID, чтобы его было куда вписать.

Счётчики подхватываются на лету: добавленный через бота или API начинает
опрашиваться в течение полуминуты, удалённый перестаёт, изменённый интервал
применяется — всё без перезапуска сервиса.

## Правила алертов

Правило задаётся одной строкой:

```
имя | метрика | направление | порог% [мин_база] [недель] [url=...]

Визиты упали        | visits  | drop | 40
Чекаут просел       | visits  | drop | 40 | url=/checkout
Заказы просели      | goal:42 | drop | 50 | 5 | 4
Каталог             | visits  | drop | 35 | url~^/catalog/\d+
Подозрительный рост | visits  | rise | 200
```

| Метрика | Что считает |
|---|---|
| `visits` | визиты |
| `users` | уникальные посетители |
| `pageviews` | просмотры страниц |
| `goals` | все достижения целей |
| `goal:<id>` | достижения конкретной цели — ID подскажет `/goals` |

**Направление:** `drop` — падение, `rise` — рост, `both` — любое отклонение.

**Порог** — на сколько процентов нужно отклониться от обычного значения для
этого времени, чтобы сработал алерт.

**`мин_база`** (по умолчанию 10) — порог значимости. Если в это время обычно
бывает 2 визита за час, то минус один визит — это «падение на 50%», но говорит
оно ни о чём. Правило молчит, пока обычное значение ниже этого числа.

**`недель`** (по умолчанию 4) — сколько прошлых недель берётся в базу. База —
медиана, а не среднее: один праздник или всплеск от рассылки не должен сдвинуть
ожидание настолько, чтобы реальное падение прошло незамеченным.

**`url=`** — ограничить правило страницами: `url=/checkout` (URL содержит
подстроку) или `url~^/catalog/\d+` (регулярное выражение). Без него правило
следит за всем счётчиком.

Считаются **сессии, в которых была хотя бы одна такая страница** — а не только
те, что на ней начались. Для `/checkout` это важно: начинают сессию на нём
единицы, а доходят многие.

> Следствие: метрика всё равно считается по сессии целиком. «Визиты с заходом на
> /checkout» — ровно то, что нужно, а вот `pageviews` с фильтром посчитает все
> просмотры таких сессий, а не только просмотры самого `/checkout`.

У правила есть кулдаун (по умолчанию 3 часа) — минимальный интервал между
повторными алертами.

## Отчёты

Периодический отчёт сравнивает сегодня со вчера, неделей и месяцем назад.

**Когда присылать** — `/schedule`:

```
/schedule 10:00   каждый день в 10:00
/schedule 6h      каждые 6 часов
/schedule off     не присылать
```

Расписание хранится в базе и меняется из чата без перезапуска. Суточный отчёт
задаётся временем суток, а не интервалом в 24 часа: интервал отсчитывается от
последнего запуска и после каждого рестарта уезжает на новое время.

**Что присылать** — `/addreport`:

```
имя [| url=...] [| goals=42,77] [| group=url] [| period=yesterday]

Чекаут             | url=/checkout
Заказы             | goals=42,77
Чекаут и заказы    | url=/checkout | goals=42
Топ страниц        | group=url
Чекаут по URL      | url=/checkout | group=url
Вчерашний чекаут   | url=/checkout | period=yesterday
Заказы за неделю   | goals=42,77   | period=7d
```

`url=` сужает отчёт до сессий, в которых была такая страница (`url~` — для регулярных выражений);
`goals=` ограничивает разбивку конкретными целями (ID покажет `/goals`);
`group=url` добавляет в отчёт блок со страницами входа (топ-10 по визитам и конверсиям);
`period=` задаёт период данных отчёта: `today` (по умолчанию, «сегодня»), `yesterday` («вчера»), `7d` («7 дней»), `30d` («30 дней»). Данные за период автоматически сравниваются с аналогичными предыдущими периодами (вчера — с позавчера, неделей и месяцем назад; 7 дней — с предыдущими 7 днями и 4 неделями назад; 30 дней — с предыдущими 30 днями).
Без отчётов присылается сводка по всему счётчику — как и раньше. Как только появился
хотя бы один отчёт, сводка прекращается: заведите её отдельным отчётом без фильтров,
если она нужна.

## Конфигурация

| Ключ | Описание | По умолчанию |
|---|---|---|
| `database` | путь к SQLite (относительный — от `~/.metrika-alert/`) | `metrika.db` |
| `telegram.bot_token` | токен от @BotFather | пусто (канал выключен) |
| `telegram.admin_ids` | ID пользователей, которым разрешено управление | `[]` |
| `telegram.proxy_url` | прокси для Telegram API (`http`, `https`, `socks5`) | нет |
| `vkteams.bot_token` | токен от @Metabot | пусто (канал выключен) |
| `vkteams.base_url` | эндпоинт Bot API | `https://myteam.mail.ru/bot/v1` |
| `vkteams.admin_ids` | ID пользователей ВК Тимс | `[]` |
| `vkteams.proxy_url` | прокси для ВК Тимс | нет |
| `vkteams.ignore_tls` | отключить проверку TLS-сертификатов (для on-premise с самоподписанным сертификатом) | `false` |
| `metrika.base_url` | хост API Метрики | `https://api-metrika.yandex.net` |
| `metrika.settle_minutes` | пауза на досчёт данных | `20` |
| `metrika.window_minutes` | ширина окна измерения | `60` |
| `metrika.step_minutes` | шаг окна и периодичность проверки | `10` |
| `metrika.max_catch_up_steps` | сколько пропущенных окон догонять за раз | `6` |
| `api.enabled` | включить REST API | `false` |
| `api.listen_addr` | адрес REST API | `:8090` |
| `api.auth_token` | обязательный bearer-токен для `/api/` | пусто |
| `report_interval_hours` | стартовое расписание отчётов, `0` — отключить. После первой команды `/schedule` значение берётся из базы | `1` |

Любой ключ переопределяется переменной окружения `METRIKA_*` — таблица в
[DEPLOY.md §9](DEPLOY.md#9-переменные-окружения).

OAuth-токены счётчиков хранятся в базе, а не в конфиге, и никогда не
возвращаются REST API. Добавляются через бота (`/addcounter`) или
`POST /api/counters`.

## Разработка

```bash
go build -mod=vendor ./...
go vet -mod=vendor ./...
go test -mod=vendor -race ./...
```

Требуется Go 1.25+ (см. `go.mod`). Зависимости завендорены — сборка работает без
сети.

### Структура

```
cmd/server        точка входа, связывание компонентов
internal/config   конфиг и переопределения из окружения
internal/model    схема SQLite, миграции, доступ к данным
internal/engine   клиент Reporting API, база сравнения, правила, отчёты
internal/alert    маршрутизация алертов по каналам
internal/bot      команды чат-бота, общие для всех транспортов
internal/vkteams  клиент Bot API ВК Тимс
internal/api      REST API
```

Логика команд написана один раз и не зависит от транспорта: Telegram и ВК Тимс
различаются только приёмом сообщений и разметкой, поэтому оба реализуют
`bot.Transport`.
